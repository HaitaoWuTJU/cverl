// CUDA kernels will be added after the LibTorch implementation is locked
// against golden tests. This file is intentionally empty so CMake can reserve
// the CUDA build path without affecting CPU-only builds.
